vars {
  baseUrl: http://localhost:3000
  ~vagaId: cole aqui o id de uma vaga aberta (vem do "Buscar vagas perto de mim")
  ~operadorId: cole aqui o id de um operador (vem do "Listar operadores")
  ~localId: cole aqui o id de um local (vem do "Listar locais")
}
